목표1 : 디스코드 채팅 to 마인크래프트
    세부목표:
    1 : ![명령어] 혹은 ! [명령어] 입력시 콘솔창에 [명령어] 적은거같이 실행
    2 : ! 안붙히고 [채팅] 만 입력시 [채팅보낸유저이름] [채팅] 을 마인크래프트 채팅창에 출력


목표2 : 마인크래프트 to 디스코드 채팅
    세부목표:
    1 : 마인크래프트에 채팅을 치면 디스코드 채널에 [마인크래프트 닉네임] [채팅] 을 출력
    2 : 마인크래프트에 /reg [지역이름] 을 치면 디스코드 채널에 x:[x좌표] y:[y좌표] z:[z좌표] : [지역이름]을 출력. 명령어가 reg가 아닌 다른것이 될 수 있음


목표3 : 마인크래프트 서버의 on/off 와 on일때 몇명접속해있는지와 접속플레이어확인
    단계목표:
    1. 디스코드채널에 !서버상태 라고 쳤을때 해당 정보들을 출력
    2. 1번을 실행하지 않아도 실시간으로 서버상태가 띄워져있음


목표4 : bat파일과 py파일 적절히사용
    세부목표:
    1 : bat 파일로 실행파일 만들기
    2 : private 정보들 관리
    3 : 디렉토리 내의 정보들을 잘 확인해서 필요에맞게 잘 수정


디렉토리 구조
server_folder
    start.bat
    spigot-x.x.x.jar
    many_files
    digNchat
        python_files
    
start.bat 파일 설계:
실행시켰을때 서버가 실행이 되었었나, eula=true가 되있는지 등 체크해서
서버가 켜질수있는 상태인지 아닌지 판정.
서버가 켜질수없으면 켜질수있게 만들기
server.properties 파일을 확인해서 rcon 관련 property들이 적절히 설정되어있는지 확인
적절히 안되있으면(rcon이 안켜져있거나 rcon비밀번호가 없거나 등)이면 적절히 설정
서버주소 등 확인
디스코드 토큰, 채널 등 확인
해당 기능들을 bat if문으로 설계할 예정
이렇게 써보니까 서버 메모리 설정같은것도 넣지않을까.

! 서버실행을 한번도 한 적이 없으면 서버설정을 먼저 해주세요
1. 서버실행
2. 서버설정
3. 도움말
4. 종료
원하는 번호를 입력하시오 : 

1을 입력했을시 로직
    서버설정을 한 적이 있는지 체크
    서버설정이 정상적으로 되어있는지를 체크

2을 입력했을시 로직
    1. 서버자동설정
    2. 서버수동설정
    3. 서버설정확인
    4. 서버설정가이드
    5. 돌아가기

    2-1 로직
        eula 파일확인, 없으면 버킷한번실행 후 eula true할건지 Y/N으로 체크
        서버파일있는지 확인 없으면 버킷한번 다른창에 실행시켜주고 stop 치라고 가이드
        그 후 server.properties 의 rcon값들 수정해주고 private.txt 값 수정
        그후 처음으로
    
    2-2 로직
        1.디스코드 봇 토큰값 수정
        2.디스코드 채널 아이디값 수정
        3.서버주소 값 수정
        4.서버포트 값 수정
        5.rcon포트 값 수정
        6.rcon비밀번호 값 수정
        7.뒤로가기
    
    2-3 로직
        2-2 값들 출력해주고
        아무값 입력하면 뒤로가기

    2-4 로직
        봇 토큰값 얻는법:
        채널 아이디값 얻는법
        서버주소 뭔지, 기본값localhost 명시
        서버포트뭔지와 기본값25565
        rcon포트 뭔지,기본값
        rcon비밀번호 뭔지
        출력하고
        아무키누르면 뒤로가기

3을 선택했을시 로직
    도움말...
    깃허브 링크

4을 선택했을시 로직
    종료

